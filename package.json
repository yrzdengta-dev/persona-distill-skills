{
  "name": "persona-distill-skills",
  "private": true,
  "version": "0.1.0",
  "description": "Local skill library for persona distillation and perspective-driven workflows.",
  "license": "MIT",
  "scripts": {
    "validate": "node scripts/validate-skills.mjs",
    "install:codex": "bash scripts/install-skill.sh all",
    "install:claude": "INSTALL_TARGET=claude bash scripts/install-skill.sh all"
  }
}
